# Триаж открытого долга: что требует сценариста, а что программиста

**Собрано 2026-09-21**, `main` = `0f8f6a3`. Источник — все разделы «за скобками»,
«не чинилось», «найдено, не чинится» в 55 документах `docs/`: **107 пунктов**, после
удаления закрытых (`✅`) и дублей между документами — **44**.

Зачем: очередь в `ENGINE13_INFRASTRUCTURE_TASKS.md` закрыта на `4` пункта из `14`, а
незакрытых находок вчетверо больше. **Большинство из них упирается не в код.** Пока это
не разделено, непонятно, сколько работы осталось мне, а сколько вам.

Столбец «цена» — грубо: **S** правка в пределах файла, **M** правка с перемером,
**L** отдельная задача со стадиями.

---

## A. Требует числа или решения сценариста — 20

Здесь я могу измерить и уже измерил. Назвать число за вас не могу.

| # | что | измерено | цена |
|---|---|---|---|
| A1 | `senator_bribe`: порог `family:wealth > 200` вдвое выше потолка шкалы | ворота невыполнимы по построению; в списке гарда | S |
| A2 | Стартовые метрики семьи rome: **три** несовпадающих набора — спецификация `8/12/22/15`, первый код `60/40/50/45`, сейчас `0/0/0/0` | восстановление оживляет `family_rises` и убивает `family_falls`; обе стороны запинены гардом | M |
| A3 | `huns_visible`: `huns.military_size > 200` при наблюдаемом максимуме `120` | мертво в обоих мирах | S |
| A4 | `milan_regency_stabilizes`: `milan.legitimacy > 65` при максимуме `55.74` | мертво в обоих мирах | S |
| A5 | `army_mutiny`: конъюнкция «армия > 100 и казна < 150» не совпадает во времени | мертво в обоих мирах | M |
| A6 | `famine` в milan: нужен актор с `economic_output < 30` | мертво в обоих мирах | M |
| A7 | **Рёбра трём спавнам constantinople** — ветка `feat/spawn-edges` готова и не влита | цель достигнута, ворота держатся, **но предсказание опровергнуто**: армия Османов `153 → 256` | L |
| A8 | Кульминация constantinople недостижима игроку (`mehmed_rises`, `final_assault`) | `10/30` и `4/30` без игрока, `0/15` с ним | L |
| A9 | `church_union`: ворота на легитимности протагониста, а игра идёт по федерации | третий класс — «ворота не на той оси» | L |
| A10 | Условие победы constantinople `ottomans.military_size < 40` | предпосылка для правки модели потерь | L |
| A11 | Рим бессмертен в безыгроковом мире; исторически Запад пал в 476 | **стадия 1 выполнена**: причина — одна строка `floor = 20.0` у ранга S, который носит один актор; `docs/investigation_rome_immortality.md` | L |
| A12 | `rome_splits` срабатывает на 4–15-м году при историческом 395 | порог не рычаг, рычаг — длительность | M |
| A13 | Ранга `C` нет в `rank_bonuses.toml` constantinople | спавны ранга `C` не получают бонуса вовсе | S |
| A14 | `mamluks` спавнится православным славянином ранга `C` | захардкожено в пути спавна | S |
| A15 | `military_quality` не читается боем | единственный поведенческий читатель — проекция силы | M |
| A16 | Два инертных правила знания; `rome.economic_output` стоит на потолке `100` с тика 6 | измерено | M |
| A17 | `plague` в constantinople почти не срабатывает | `0.1` срабатывания за партию | S |
| A18 | Тег-канал — крупнейший производитель `ep` во всех трёх сценариях | `(C)` ратифицирован; пересмотр — ваше слово | L |
| A19 | Второй цикл `cohesion ↔ legitimacy` | держит `ostrogoth_kingdom` в зоне коллапса 76.8 % жизни | L |
| A20 | Абсолютные дельты `population` в общем пуле событий; эффекты `flood` по `cohesion`/`eo` | `(C)` по населению доказан, по когезии не мерился | M |

## B. Чистый код — 24 (B2 сделан, B3 опровергнут, B4 → A22, добавлены B26/B27)

Здесь решение не нужно: либо дефект, либо мёртвое.

| # | что | цена |
|---|---|---|
| B1 | `tick_span` / `tempo` / `tick_label` инертны — движок хардкодит `tick / 2` | S |
| B2 | ✅ **СДЕЛАНО**, объём оказался шире: живой крейт `src-tauri` вне воркспейса и вне CI | S→M |
| B3 | ❌ **ОПРОВЕРГНУТ**: писателей три, в `src-tauri/src/main.rs`; заменён на B26 + B27 | — |
| B4 | → **A22**: массив — события одного тика, порядок — авторский выбор, не дефект | — |
| B5 | ✅ **СДЕЛАНО**: поле добавлено в зеркало TS; показ панелью — отдельный авторский вопрос | S |
| B6 | ✅ уже закрыт в PR #46 вместе с `llm_trigger` — в коде не встречается | — |
| B7 | ✅ **СДЕЛАНО**: параметр снят с обеих сторон, убран дубль `tick % 2` во фронтенде | S |
| B8 | ✅ **СДЕЛАНО**: паттерн без слеша; проверено на пустом репозитории с обеих сторон | S |
| B9 | `metrics_to_snapshot` — сортировка, которая ничего не сортирует | S |
| B10 | ✅ **СДЕЛАНО**: сигнал «кризис» был завышен в 16…44 раза, инструмент оценки заражён | S |
| B11 | Дедупликация отбора релевантности по **неуникальному** `id` | M |
| B12 | Правило 3 отбора не видит `actor_id = "scenario"` | M |
| B13 | `(C)` **отказ**: поглощение видно через блок гибели и запись метрик; переписан как A24 | — |
| B14 | ✅ **СДЕЛАНО**: спавн — `Birth`; в блок «события периода» попадало 0 из 16, стало 16 из 16 | S |
| B15 | ✅ **СДЕЛАНО** + уровнем ниже: объяснение было пусто 0 из 60 тиков; подсистема без потребителя → A23 | S |
| B16 | `budget_probe` `(D₆)` считает наследников формулой, которой движок не исполнял | M |
| B17 | `erosion_probe` и `vassalage_probe` гоняют rome без `family_state` — смена поколений молча выключена | M |
| B18 | `core_tests.rs` сеет сырые family-ключи (класс §5.H) | M |
| B19 | Промпт печатает идентификаторы метрик и тут же запрещает их упоминать | S |
| B20 | `status_indicators`: пороговые ярлыки не доходят до летописца | M |
| B21 | 20 несортированных обходов `world.actors` не классифицированы | M |
| B22 | `(C)` **отказ**: вызовов два и оба инертны, а путь спавна её не зовёт — правка меняет баланс | — |
| B23 | ✅ уже закрыт в PR #47: обе коллекции пишутся вместе, ни одна проверка не вырождена | S |
| B24 | Поглотитель не получает рёбра поглощённого | M |

---

## Что из этого следует

**Две трети открытого — ваши.** Двадцать пунктов требуют числа или решения о замысле, и
ни одно измерение их не заменит. Двадцать четыре — мои, и из них **тринадцать `S`**:
правка в пределах файла, вместе они меньше одной задачи из очереди.

**Исходная постановка откладывала четвёртый сценарий и v2-системы до закрытия долга.**
Этим маршрутом долг не закроется: колонка A не уменьшается от того, что я продолжаю
мерить. Разумный порядок — закрыть колонку B пачками по теме, а колонку A прочитать
целиком и решить, что из неё вообще нужно закрывать, а что признать свойством мира.

**Чего в списке нет:** пунктов, найденных гардами и сразу же починенных, — их
восемнадцать, и они уже в `main`. Список открытого перестал расти только тогда, когда
гарды стали ловить дефекты при их внесении, а не при следующей переписи.


---

# Решения владельца, 2026-09-21

Колонка A закрыта в большей части. Ниже — как записано, включая одну поправку, где
проверка не подтвердила посылку.

## Принято и закрыто решением — 8

| # | решение |
|---|---|
| A1 | `senator_bribe`: порог **`>= 60`**. Подкуп сенатора дешевле найма армии (`>= 100`), а не вдвое дороже потолка; достижим после одного `raise_taxes` |
| A3 | `huns_visible`: порог **`>= 90`** — верхняя четверть наблюдаемого максимума `120`. Порог называется от измеренного, а не от воображаемой шкалы |
| A4 | `milan_regency_stabilizes`: порог **`>= 48`**, та же арифметика от максимума `55.74` |
| A5 | `army_mutiny`: **свойство мира**. Мятеж при большой армии и пустой казне — осмысленное совпадение, которое не случается; разводить конъюнкцию значит выдумывать событие |
| A6 | `famine` в milan: **свойство мира**. Ломбардия 1477 — богатейшая; `economic_output < 30` там и не должен достигаться |
| A12 | `rome_splits`: цель — раздел около **20-го года** (395). Рычаг — длительность |
| A17 | `plague` `0.1` за партию: **свойство мира**. Чума раз в десять партий и есть чума |
| A11 | Бессмертие Рима: **дефект, чинить, приоритет выше всего остального в A.** Ноль гибелей в `30 × 13` значит, что сценарий не умеет рассказать свою историю; падение Запада в 476 — премисса, а не деталь |

## Константинополь: три пункта — одно решение

**Победа переносится с `ottomans.military_size < 40` на федерацию.** Город падёт — это
история; сценарий не про «можно ли выстоять», а про «сколько продержишься и какой ценой».
Победа — собранная коалиция, а не разбитая армия.

| # | следствие |
|---|---|
| A9 | **закрыт** — ворота приходят на ту ось, по которой идёт игра |
| A10 | **закрыт как предпосылка** — модель потерь перестаёт быть заложницей условия победы |
| A8 | **меняет природу**: `mehmed_rises` и `final_assault` перестают быть «недостижимой победой» и становятся кульминацией падения, которую игрок должен увидеть. Сделать достижимыми, но **не как выигрыш** |

## Переехало в B — 4 пункта, 3 строки

| было | стало |
|---|---|
| A15 `military_quality` не читается боем | **B25** — не выбор числа, а шестой класс: метрика показывается игроку как влияющая и не влияет. Чинить, но не сейчас |
| A16 инертные правила знания; `rome.eo` на потолке с тика 6 | **B26** — метрика, упершаяся в потолок на шестом тике из трёхсот, не решение сценариста |
| A20 абсолютные дельты `population` | **B27** (по населению доказан); половина по `cohesion` **остаётся в A** — не мерилась |

## Поправка: A13 + A14 — половина в B, половина обратно в A

Владелец объединил их в один дефект «движок назначает спавнам ранг, которого сценарий не
описывает» и перенёс в B как «числа не нужны». **Обе половины подтвердились, но проверка
показала, что дефект шире, и вторая половина числа требует.**

| сценарий | акторов ранга `C` | объявлено в `rank_bonuses.toml` |
|---|---|---|
| rome_375 | **8 — самая большая группа из 22** | `S A B D` |
| constantinople_1430 | 3 | `S A B D` |
| milan_1477 | 2 | `S A B D` |

**Ранг `C` носят 13 авторских акторов в трёх сценариях, и бонуса для него нет нигде.**
Это не про спавны: спавны лишь добавляют к этой группе ещё троих, потому что путь спавна
прибивает `RegionRank::C` (`mod.rs:993`) независимо от контента.

Отсюда деление:

* **B28 — путь спавна прибивает ранг.** Ранг должен приходить из конфига спавна, как
  приходят соседи и метрики. Чисто код.
* **A21 — какой бонус получает ранг `C`.** Это эффекты и числа, то есть авторское
  решение, и оно касается **13 акторов**, а не трёх спавнов.

Попутно: из 12 правил бонусов в трёх сценариях **3 мертвы** — `S` и `D` в
constantinople, `S` в milan (§4 переписи молчаливого контента). Таблица описывает ранги,
которых нет, и не описывает тот, который есть.

## Остаётся в A — 6

| # | что | почему не решено |
|---|---|---|
| A2 | Стартовые метрики семьи — выбран набор `8/12/22/15` | **набор никогда не мерился**; идёт со стадией 2 и замером размена |
| A7 | Рёбра спавнам | мержить после замера `mehmed_rises` / `final_assault` **в сыгранном мире**: база там `0/15`, и если рёбра двигают и это, об этом надо объявить, а не обнаружить |
| A18 | Тег-канал | `(C)` ратифицирован, пересмотр требует стадии 1, которой нет |
| A19 | Второй цикл `cohesion ↔ legitimacy` | `76.8 %` жизни в зоне коллапса выглядит дефектом, но это **один** актор; нужен обход по всем акторам трёх сценариев |
| A20b | Эффекты `flood` по `cohesion` | не мерились |
| A21 | Бонус ранга `C` | см. поправку выше |

## Счёт

| | было | стало |
|---|---|---|
| A — требует сценариста | 20 | **6** |
| B — чистый код | 24 | **28** |
| закрыто решением | — | 8 |
| признано свойством мира | — | 3 |

**Порядок работ, заданный владельцем:** тринадцать дешёвых пунктов B первыми; `A11`
(бессмертие Рима) — приоритет выше всего остального в A.


---

# Поправка к B9: объём больше, и она задевает главный инструмент доказательства

Пункт был записан как «сменить тип `Event::metrics_snapshot` через `Event` и фронтенд».
**Объём больше, и последствие шире самого пункта.**

## Порядок ключей плывёт между процессами

`HashMap` с хэшером по умолчанию рандомизирует порядок обхода **на процесс**.
Воспроизведено независимо — один бинарник, одни данные, три запуска:

```
legitimacy,population,economic_output,cohesion,military_quality,treasury,military_size…
population,military_size,external_pressure,cohesion,legitimacy,treasury,military_quality…
cohesion,treasury,military_quality,population,economic_output,legitimacy,military_size…
```

`serde_json` пишет в порядке обхода. Значит **JSON при одном сиде и одном мире выходит
разным по байтам от запуска к запуску.**

## Наружу через `serde_json` уходит не один снимок

| где | что |
|---|---|
| `save_load.rs` | `world_state` целиком — внутри `actors`, у каждого `metrics` и `scenario_metrics` |
| `save_load.rs` | `family_state` |
| `actions.rs` | `applied_effects` |
| `db.rs` | `metrics_snapshot`, `final_metrics` событий и мёртвых акторов |

Сериализуемых `HashMap` в публичных типах — **14**.

## Почему это важнее самого пункта

**Побайтовое сравнение — главный инструмент доказательства «правка не сделала лишнего».**
Оно применялось в пункте B (морской тег), при вводе приёмника трассировки, при
переименовании семьи, при правке таблицы рангов.

> **Оно достоверно для вывода `sim` и недостоверно для всего, что прошло через
> `serde_json` от `HashMap`.** Там отрицательный результат означает не «изменилось», а
> «хэшер перетасовал».

Это ровно та мина, что уже записана дважды: **отрицательный результат пробы против
пробы, которая не состоялась.** Третий её вид.

**Ни один прошлый вывод не задет, и это проверено:** все побайтовые сверки в этой работе
шли по стандартному выводу `sim`, который строится форматированием, а не сериализацией;
а промпты летописца сделаны детерминированными отдельной задачей, где пять обходов
`HashMap` были явно отсортированы.

## Пункт переписан

**B9′ — сериализуемые карты должны быть упорядочены.** Сменить `HashMap` на `BTreeMap`
везде, где значение уходит наружу через `serde_json`: `Event`, `Actor`, `WorldState`,
`DeadActor`, эффекты действий. Затрагивает Rust-типы, TS-зеркало и формат сейвов.
**Цена L**, не S, и до неё побайтовая сверка сейвов и строк базы недостижима **по
построению**.

## Дополнения владельца к B9′ (2026-09-21)

Два пункта входят в объём B9′ сверх записанного выше:

1. **Порядок панелей на экране.** Смена `HashMap` на `BTreeMap` меняет не только байты
   JSON: панели, которые обходят карту метрик как есть (`FamilyPanel.tsx` и два
   соседних), после правки покажут метрики в алфавитном порядке вместо случайного.
   Это видимое изменение продукта, а не внутренняя чистка, и должно быть заявлено.
2. **Гард: «ни один публичный сериализуемый тип не держит `HashMap`».** Без него
   правка держится один коммит.

**Замечание о методе, которое владелец сформулировал отдельно:** класс был невидим
потому, что **инструмент поиска сам был заражён тем классом, который должен был найти** —
порядок ключей плывёт, а сравнение по байтам считали исправным. Формулировка
«строится форматированием, а не сериализацией» сама по себе ещё не довод: её надо
проверять на каждом пути вывода.

**Проверено дополнительно (2026-09-21):** пакетный режим `sim` тоже чист, и не потому что
в нём нет обхода `HashMap`. Обход есть — `sim.rs:1115` `for actor in world.actors.values()`, —
но он копит в `actor_finals`, а печать идёт после `names.sort()` на `sim.rs:1195`.
Одиночный режим сортирует явно на `sim.rs:439`. `serde_json` в `sim.rs` не встречается ни разу.

---

# B2 переписан: дело не в дубле, а в невидимом крейте

Пункт был записан как «дублирующая мёртвая точка входа `commands::cmd_get_narrative`».
Дубль есть, но он — следствие. Причина крупнее и объясняет ещё два пункта очереди.

## Живой код приложения не компилируется ни одной проверкой

`cargo metadata --no-deps` даёт **одного** члена воркспейса: `engine13`. Крейт
`src-tauri` (431 строка, 23 команды Tauri) в воркспейс не входит, поэтому
`cargo test --workspace` и `cargo clippy --workspace` его **никогда не собирали**.

Хуже: `src-tauri/.cargo/config.toml` — файл в репозитории — задавал

```toml
rustc = "/home/deck/.cargo/bin/rustc"
cargo = "/home/deck/.cargo/bin/cargo"
```

то есть живой крейт **не собирался нигде, кроме одного ноутбука**, даже если бы CI его
попробовал. Пин удалён; комментарий в файле объясняет, почему его нельзя возвращать.

## Что из этого выросло

**Перепись 23 команд:** 17 делегируют в библиотеку, **6 держат собственную логику**
(85 строк), крупнейшая — `cmd_get_narrative`, 39 строк.

**Четыре функции библиотеки названы как зарегистрированные команды**, и ни одну из них
живая команда не зовёт:

| функция библиотеки | вызовов в продукте |
|---|---|
| `commands::cmd_get_narrative` → `application::narrative::cmd_get_narrative` | 0 |
| `commands::cmd_get_available_models` | 0 |
| `commands::cmd_save_llm_config` | 0 |
| `commands::cmd_get_map_config` | 0 |

Все четыре удалены (вместе с `src/application/narrative.rs` целиком и реэкспортами).
112 тестов проходят — компилятор и есть доказательство, что вызовов не было.

**Расхождение уже накопилось, молча.** Мёртвая `commands::cmd_get_map_config` несла
собственный атрибут `#[tauri::command]` и ждала `tauri::State<'_, tokio::sync::Mutex<AppState>>`,
а приложение регистрирует `std::sync::Mutex<AppState>`. Копия компилировалась, проходила
clippy и была непригодна к подключению. Это и есть цена мёртвого двойника: он выглядит
покрытым.

## Отзыв двух записанных утверждений

`db.rs::get_relevant_events_scored` и `llm/mod.rs` утверждали как измеренный факт:

> таблица `events` не пишется никем, кроме `budget_probe`; в продукте этот метод читает
> пустое хранилище

**Это неверно.** Писатели есть — `src-tauri/src/main.rs:60`, `:99`, `:184`
(`cmd_advance_tick`, `cmd_advance_tick_silent`, `cmd_submit_action`), каждый зовёт
`insert_events_batch`. Утверждение было получено поиском по `src/`, который крейта не видит.
Оба комментария исправлены в коде.

Что это меняет и что нет: правка окна событий (PR #43) чинила `truncate(10)`, хранивший
**самые старые** десять, — она остаётся верной. Неверным было обоснование, почему
питатель взят из памяти, а не из базы: это теперь **выбор, а не единственная
возможность**. Заведено отдельным пунктом.

Попутно: `main.rs:184` вставляет в таблицу **весь журнал целиком** на каждом поданном
действии, а не приращение. Не мерено, не тронуто — новый пункт B27.

**Это в точности наблюдение владельца по B9′, второй его случай: инструмент поиска был
заражён тем классом, который должен был найти.** Там — порядок ключей, здесь — граница
воркспейса.

## Сделано

- Четыре мёртвые функции и модуль `application/narrative.rs` удалены.
- Пин путей из `src-tauri/.cargo/config.toml` убран.
- Задание CI `tauri`: `npm ci && npm run build` (нужен `../dist` для `tauri-build`),
  системные зависимости, `cargo check --manifest-path src-tauri/Cargo.toml`.
  **Это первая проверка живого кода приложения за всё время.**
- Три гарда в `tests/scenario_conventions.rs`, все проверены инъекцией (падают 3/3):
  `library_defines_no_twin_of_a_live_tauri_command`,
  `tauri_command_attribute_lives_only_in_the_tauri_crate`,
  `cargo_configs_contain_no_machine_specific_paths`.
- Раздел «Where the product's code lives» в `AGENTS.md`.
- Два ложных комментария исправлены.

## Замечено и оставлено

- `tauri.conf.json` несёт ту же болезнь мягче: `beforeDevCommand` и `beforeBuildCommand`
  делают `export PATH=/home/deck/.cargo/bin:$PATH`. На чужой машине это безвредно
  (несуществующий каталог в начале `PATH`), и удаление может сломать запуск с рабочего
  стола у владельца. Названо, не тронуто.
- Зарегистрированы, но фронтендом не вызываются: `cmd_get_tick_explanation`,
  `cmd_set_game_mode`. 21 имя `invoke(...)` во фронтенде — все зарегистрированы.
- `clippy` по `src-tauri` в задание CI **не** добавлен: 431 строка никогда не линтовалась,
  и чинить лишки в коде, который я не могу собрать локально, значит править вслепую.
  Отдельным пунктом после того, как `cargo check` станет зелёным.

---

# B3 опровергнут в записанной форме

«Таблица `events` без писателей в продукте» — писатели есть, три, см. выше. Пункт
закрывается как ошибка постановки; на его место встают два новых:

| id | пункт | цена |
|---|---|---|
| B26 | Питатель отбора релевантности: память против таблицы — теперь выбор, и он не обоснован | M |
| B27 | `cmd_submit_action` вставляет в `events` весь журнал на каждом действии, а не приращение | S |

---

# B4: не дефект кода, а невысказанное решение

`ControlPanel.tsx:59` — `recentEvents.slice(-3).reverse()`. Массив приходит из
`App.tsx:259` `setRecentEvents(response.events)`, то есть это события **одного тика**,
а не накопленный журнал. «Последние три по порядку массива» для одного тика — это
«три последних события этого полугодия», и такая панель не обязана быть отбором по
релевантности.

Механической правки здесь нет: любое изменение — это выбор, какой порядок увидит игрок,
и он авторский. Пункт переводится из B в **A22** без цены.

---

# B10: сигнал «кризис» на 95…99 % состоял из погибших

`collapse_warning_ticks` — счётчик подряд идущих опасных тиков, три подряд означают
коллапс. Сбрасывается он в одном месте: когда актор **вышел** из опасности
(`engine/mod.rs:1789`). Погибший из опасности не выходит — он исчезает из
`world.actors`, а цикл идёт по живым, и его запись больше никто не трогает.

## Замер до правки

3 сценария × 5 сидов × 300 тиков, без игрока:

| сценарий | тиков со списком | из них с погибшими | **только погибшие** |
|---|---|---|---|
| rome_375 | 133…252 | 127…250 | **125…238** |
| constantinople_1430 | 143…267 | 140…265 | **134…255** |
| milan_1477 | 230…257 | 228…253 | **224…251** |

## Почему это не только утечка в сейве

Ключи этой карты читает `sim.rs:636` и кладёт в обзорную пачку как `collapse_warnings`.
Отбор случаев (`sim.rs:670`) объявляет: «„кризис“ — это то, что движок сам пометил через
`collapse_warning_ticks`». После первой же гибели пометка не снималась никогда, поэтому
инструмент оценки нарратива называл кризисом почти любой тик.

**Это третий случай той же мины: инструмент измерения заражён дефектом, который должен
измерять.** Первый — порядок ключей `HashMap` под побайтовой сверкой (B9′), второй —
поиск по `src/`, не видящий живого крейта (B2).

## После правки

Одна строка в конце `check_collapses`: оставить только ключи живых акторов.

| сценарий | тиков со списком | осиротевших записей |
|---|---|---|
| rome_375 | 4…14 | **0** |
| constantinople_1430 | 7…13 | **0** |
| milan_1477 | 4…6 | **0** |

Настоящая опасность — 1…5 % тиков, а не 44…89 %. Завышение было в 16…44 раза.

`sim … 300 batch` по трём сценариям **побайтово** совпадает с базой: правка не трогает
ход симуляции, только осиротевшее состояние и выведенный из него сигнал.
Гард `test_collapse_warnings_hold_only_living_actors` проверен инъекцией и сначала
утверждает, что гибели были — иначе он охраняет пустоту.

---

# B5, B6, B7 — закрыты

| id | что оказалось |
|---|---|
| B5 | `generation_count` был пропущен в зеркале `src/types/index.ts`. Поле добавлено; **ни одна панель его не показывает** — это отдельный (авторский) вопрос, зеркало теперь честное |
| B6 | Уже закрыт: `ticks_since_last_narrative` удалён вместе с `llm_trigger` (PR #46). В коде не встречается ни разу |
| B7 | `_half_year` удалён целиком — с обеих сторон. Фронтенд считал полугодие **вторым способом** (`App.tsx` `tick % 2`) и слал в параметр, который команда игнорировала: движок берёт его из снимка. Заодно снят дубль арифметики движка в TypeScript |

B7 стал возможен только теперь: правка трогает `src-tauri/src/main.rs`, и до задания
`tauri` в CI проверить её было нечем.

---

# B13 / B14 / B15 — жизненный цикл в журнале

## B14: появление державы не доходило до летописца

Спавн писался как `EventType::Milestone`. Блок «события периода» в `build_snapshot`
разбирает только `Death` и `Birth`; в список вех спавн тоже не попадал, потому что
`key_milestones_fired` строится из `scenario.milestone_events`, а синтетического
`spawn_*` там нет. До летописца он доходил только случайным попаданием в окно
релевантности.

Контрфакт, 3 сценария × 5 сидов × 300 тиков:

| сценарий | спавнов | в блоке при `Milestone` | в блоке при `Birth` |
|---|---|---|---|
| constantinople_1430 | 11 | **0** | **11** |
| milan_1477 | 5 | **0** | **5** |
| rome_375 | 0 | — | — |

Тип изменён на `Birth` — тот же, которым помечено рождение наследника, с теми же
тегами, чтобы канонический отбор оценивал событие так же. Гард
`test_spawned_power_reaches_the_lifecycle_block` требует сперва, чтобы спавн вообще
случился.

## B15: дефект внутри функции, которая всегда возвращала пустоту

Запись пункта — «`TickExplanation.collapses` читает `EventType::Collapse`, а это маркер
режима, не гибель» — верна: этот вариант пишется в одном месте, для вехи сценария с
`triggers_collapse`, и несёт `actor_id = "scenario"`. Поле никогда не называло актора.

**Но уровнем ниже функция не возвращала вообще ничего.** `generate_tick_explanation`
фильтрует журнал по `world.tick`, а тот уже увеличен фазой `phase_advance`. Замер
(milan_1477, 60 тиков): журнал держит тики 0…59 при `world.tick = 60`, **непустых
объяснений 0 из 60** — пусты все четыре списка, не только `collapses`.

**Четвёртый случай той же мины:** пункт был записан на том уровне, который видно
читателю, а настоящее состояние лежало на уровень ниже.

Исправлено и то и другое: фильтр по прошедшему тику, `collapses` читает
`EventType::Death`. Гард `test_tick_explanation_describes_the_tick_that_just_ran`
сначала требует непустого объяснения, потом — что гибели были, и только потом
проверяет имя.

### Замечено и оставлено

Подсистема отладки не имеет потребителя: `TickExplanation` **не зеркалится в
TypeScript**, `cmd_get_tick_explanation` зарегистрирован, но фронтендом не вызывается.
То есть починенная функция по-прежнему никуда не ведёт. Это решение владельца —
подключить панель отладки или удалить подсистему; заведено как **A23**.

## B13: «не логируется» — слишком сильно сказано

Поглощение наследником-живой-державой встречается часто: 5 сидов × 300 тиков —
**10 гибелей из 20 в constantinople** и **5 из 10 в milan** (в rome ноль).

Но оно не молчит:

1. Летописец узнаёт о нём из блока гибели: `period_lifecycle` для `Death` перечисляет
   `successor_ids`, отфильтрованные по `world.actors`, а живой поглотитель этот фильтр
   проходит.
2. Прибавка `expansion_count` попадает в потиковую запись изменений метрик.

Чего нет — отдельного события на линии самого поглотителя. Добавление события меняет
журнал, а значит и окна релевантности, ради разрыва, который уже закрыт двумя путями.
**Вердикт `(C)`: названо, не добавлено.** Пункт переписан как A24 — вопрос авторский:
должна ли экспансия быть событием самой державы.

---

# B22 / B23 / B8

## B22: функцию зовут там, где она инертна, и не зовут там, где нужна

`ensure_default_metrics` вызывается в движке дважды — оба раза для актора из
`scenario.actors`. Перепись: **все 44 авторских актора трёх сценариев (включая
шаблоны наследников) несут все восемь ключей по умолчанию**. Вставлять нечего, оба
вызова инертны.

Единственное место, где актор строится из неполной карты, — спавн
(`engine/mod.rs` ~1010): метрики берутся из `cfg.initial_metrics`, и вызова там нет.

| спавн | задано | не задано |
|---|---|---|
| `wallachia` | 6 | `external_pressure`, `treasury` |
| `poland_lithuania` | 6 | `external_pressure`, `treasury` |
| `mamluks` | 6 | `external_pressure`, `treasury` |
| `france` (milan) | 8 | — |

`Actor::get_metric` на отсутствующем ключе возвращает **`0.0`**, а не значение по
умолчанию. То есть три державы constantinople входят в мир с `ep = 0` и `treasury = 0`.

**Контрфакт (добавить вызов на пути спавна) — не байт-в-байт:** rome и milan совпадают,
constantinople расходится — коллапсы `100 % → 99 %`, медиана гибели тика `44 → 40`,
`byzantium` `100 → 99` гибелей, появляется гибель `ottomans`. Это **правка баланса**,
а не уборка. Задача 25 уже рассматривала полноту спавнов и **отклонила**
`external_pressure` у них как неверный рычаг (значение вне шкалы игры).

**Вердикт `(C)`: не трогать.** Вместо правки — гард
`default_metric_coverage_is_pinned_on_both_sides`, закрепляющий **обе стороны**:
авторские акторы полны, спавны — нет, и ровно эти три ключа. Если изменится любая
сторона, это увидят.

## B23: уже закрыт

«Идемпотентность пути наследника не смотрит в `dead_actors`» — смотрит. Оба пути
наследника проверяют `world.dead_actor_ids` (`engine/mod.rs:1186`, `:1882`), путь
спавна — `world.dead_actors` (`:983`). Обе коллекции пишутся **в одном месте и
вместе** (`:1850`, `:1851`), так что ни одна проверка не вырождена. Закрыто гардом
воскрешения из PR #47.

## B8: паттерн со слешем не ловит симлинк

`node_modules/` совпадает только с каталогом. В worktree `node_modules` бывает
симлинком, и тогда он попадает в `git status`.

Проверено на пустом репозитории, обе стороны: с `node_modules/` симлинк **не
игнорируется** (`git check-ignore` пуст, `git status` показывает `?? node_modules`);
с `node_modules` игнорируется и симлинк, и обычный каталог с файлом внутри.
Заодно убран дубль `/target`.


---

# Решения владельца 2026-09-22 и что по ним сделано

## A11 — закрыта в три шага

| шаг | что | где |
|---|---|---|
| 1 | место раскола усыновляет список наследников вместе с именем | PR #122 |
| 2 | пол легитимности ранга S `20.0 → 12.0` | PR #123 |
| 3 | ранг места — **A25**, отдельный пункт, не начат | §10 расследования |

Поправка к расследованию: §5 утверждал, что `rome_west` и `rome_east` входят в мир
впервые. Для `rome_east` это неверно — раскол срабатывает в 25 прогонах из 30 при
нетронутом полe. Ошибка была моя, числа стояли в переписи базового мира.

| id | пункт | цена |
|---|---|---|
| A25 | Место раскола усыновляет имя шаблона, но не его **ранг**: огрызок носит `S` и получает пол, написанный для неразделённой империи. Усыновление ранга делает Рим смертным 25/30 **без правки контента**, но Запад умирает через четыре года после раскола | M |

## A23 — удалено

Подсистема отладки снята целиком: `TickExplanation` и пять её структур,
`generate_tick_explanation`, `commands::get_tick_explanation`, команда
`cmd_get_tick_explanation` и её регистрация, реэкспорт в `lib.rs`.

Довод: функция возвращала пустоту 60 раз из 60, и это никого не разбудило —
определение подсистемы без потребителя. Зеркала в TypeScript у `TickExplanation`
не было, фронтенд команду не вызывал. Починенная функция, которую никто не зовёт,
сгниёт снова и так же тихо. Измерение обслуживает `engine::trace`, у которого
потребители есть. Решение обратимо: коммит остаётся в истории.

## A24 — событие не добавлено, имя дописано

Вердикт `(C)` подтверждён: информация о поглощении доходит двумя путями, а новое
событие сдвинуло бы окна релевантности.

Настоящая дыра была в тексте: `"Держава {} прекратила существование"` **не называла
поглотителя никогда**. Теперь при живом наследнике текст продолжается — «её земли
отошли державе X». Ни нового события, ни сдвига окон.

| сценарий | гибелей | из них с именем поглотителя |
|---|---|---|
| rome_375 | 33 | 0 (поглощений живой державой там нет) |
| constantinople_1430 | 20 | **10** |
| milan_1477 | 10 | **5** |

> Держава Византийская Империя прекратила существование, её земли отошли державе
> Османская Империя

Гард `test_death_text_names_a_living_heir` сначала требует, чтобы поглощение было.

## B19 + B20 — не один пункт

Наборы не совпадают: `key_metrics` в rome 6 штук, пороговыми индикаторами покрыта
**одна**. Замена одного на другое не закрывает два пункта, а меняет то, о чём летописец
вообще знает. Решение: **сохранить набор `key_metrics`, сменить рендер** — печатать
порог, а не число, и дописать недостающие словари порогов в контент. Одним пунктом это
станет только когда словари есть для всех шести.

Попутно: индикатор несёт `invert` — единственное место, где сказано, в какую сторону
метрика плохая; `key_metrics` этого не несёт вовсе.


---

# B19 сделан; B20 — всё ещё не тот же пункт

## Перепись

| сценарий | `key_metrics` | покрыто индикатором |
|---|---|---|
| rome_375 | 6 | **1** |
| constantinople_1430 | 5 | 3 |
| milan_1477 | 5 | 3 |

Итого 16 метрик, 7 покрыты, **9 словарей написано заново**.

## Что решило, куда класть словари

Индикатор «Баронская фронда» в milan читает `naples.cohesion` и называет **высокое**
значение «мятеж». Для самого Неаполя то же число означало бы «единство держится».
Значит словарь принадлежит **не метрике, а тому, о ком речь**, и механического
отображения «метрика → слово» не существует.

Отсюда устройство: у каждой ключевой метрики свой `label` и свой словарь полос в
контенте (`core::KeyMetric`), а где на ту же метрику смотрит ещё и индикатор, слова
обязаны совпадать — иначе игрок увидит в панели одно, а в хронике прочтёт другое из
одного числа. Закреплено гардом с обеих сторон.

## Что стало в промпте

```
Положение дел:
  Семья Анициев: незначительна
  Учёность Анициев: скудна
  Богатство Анициев: велико
  Связи Анициев: оборваны
  Власть Рима: оспаривается
  Единство Рима: держится
```

Было: `actor:rome.legitimacy: 40.5` — движковый ключ и число, под системным запретом
«НИКОГДА не называй числа» и «НИКОГДА не пиши „актор X имеет Y единиц"».

**Направление больше не нужно флагом.** `invert` существует для полоски в интерфейсе;
в строке промпта направление несут сами слова: `external_pressure: 78.3` не говорит
модели ничего, «Константинополь: критическое положение» говорит всё.

## Найдено по дороге: словарь превращал гибель в спокойствие

`MetricRef::Actor::get` на отсутствующем акторе возвращает `0.0`, и у перевёрнутой
метрики ноль попадает в **благополучную** полосу. На сиде 42, тик 40, Византия
мертва — а блок сообщал:

```
  Константинополь: держится
```

Число `0.0` было равно бессмысленно и до полос, но словарь превратил его в **ложное
утверждение**, то есть ровно в то, что запрещают жёсткие правила промпта. Теперь
метрика павшей державы из блока выпадает; павшие у летописца и так есть отдельным
списком. Гард `the_world_state_block_omits_a_fallen_power` сначала требует, чтобы
Византия на этом сиде действительно погибла.

## Побочно закрыт кусок B9′

`NarrativeWorldSnapshot.key_metrics` был `HashMap<String, f64>` — один из
сериализуемых `HashMap`, и его приходилось сортировать по ключу при каждом чтении
(`llm/mod.rs`, `sim.rs`). Теперь это `Vec` в авторском порядке: порядок детерминирован
по построению, обе сортировки сняты.

## B20 остаётся отдельным

Пороговые ярлыки индикаторов доходят до летописца **только там, где индикатор и
ключевая метрика смотрят на одно число** — 7 случаев из 16. Остальные девять несут
словари, написанные для хроники. B20 в исходной формулировке («ярлыки индикаторов не
доходят до летописца») не закрыт: до него доходят не ярлыки, а полосы, и совпадают они
по построению только в семи случаях.
