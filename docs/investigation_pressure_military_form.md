# Форма зависимости `external_pressure → military_size`

**Стадия 1 — обход и измерение, без правок движка и контента.**
База: `main` = `c153440`. Ветка свидетельства: `investigate/pressure-military-form`
(**NOT FOR MERGE**: трассировка зависимостей в движке, режим `ExcessProportional`,
`dependency_probe`, переключатели в `sim`).

Постановка из очереди: «абсолютная потиковая величина, применяемая к армиям,
различающимся в сорок раз». Эта формулировка проверена и оказалась **заниженной**:
разброс цены правила по акторам — **190× / 124× / 234×** в rome / constantinople /
milan.

---

## 1. Обход, а не поиск одного правила

Искать «то самое правило» — значит заранее знать ответ. Пространство, в котором
живёт дефект, перечислено целиком: **все 47 правил зависимостей трёх сценариев**
(`rome_375` 16, `constantinople_1430` 15, `milan_1477` 16), классифицированные по
**шкале цели**, потому что именно шкала решает, значит ли абсолютная дельта одно и
то же для Рима и для Урбино.

Шкала берётся не на глаз, а из движка: `phase_clamp` держит `legitimacy`,
`cohesion`, `military_quality`, `economic_output`, `external_pressure` в `0..100`;
`military_size` и `population` — только снизу; `treasury` не клампится вовсе.

| класс цели | правил (на сценарий) | правила |
|---|---|---|
| ограничена `0..100` — одна шкала для всех | 12 | двенадцать правил, включая три остальных выхода `external_pressure` |
| безгранична, цена **на запасе цели** | 2 | `economic_output_to_population`, `low_economic_output_to_population_decay` — переведены задачей 22 |
| безгранична, цена **абсолютной дельтой** | **2** | **`external_pressure_to_military_size`**, `economic_output_to_treasury` |

**Класс дефекта содержит ровно два правила, и оба — остаток той же правки, которую
задача 22 довела только до `population`.** Второе (`economic_output_to_treasury`)
в объём этой задачи не входит и заведено «за скобками» (§10).

## 2. Инструмент

`dependency_probe` (NOT FOR MERGE) читает **собственную трассировку движка**
(`engine::dep_trace_*`): потоковый приёмник, по умолчанию выключенный, пишет
каждую применённую дельту в тот момент, когда движок её применил. Это существенно:
фаза зависимостей идёт **внутри** `phase_interactions`, после авто-дельт, рангов и
военного восстановления, так что снимок на границе тика — не то, что видят правила.

Ничего в пробнике не пересчитывает формулу заново. Это не стиль, а урок: две
рукописные копии `apply_dependency_rule` уже существуют в `budget_probe` (§10).

Контрфакты задаются на стороне сценария (пробник владеет объектом `Scenario`,
движок не трогается, ни один бросок RNG не сдвигается):
`E13_DEP_OFF`, `E13_DEP_COEF`, `E13_DEP_MODE`. При пустом окружении выход
побайтово равен базе — проверено.

## 3. Что правило делает сегодня (30 сидов × 300 тиков, без игрока)

Правило: `mode = excess`, `threshold = 50`, `coefficient = 0.01` ⇒ при насыщенном
давлении (`ep = 100`) изъятие равно **ровно 0.5 армии за тик, одинаково для всех**.

| сценарий | доля актор-тиков, где правило срабатывает | изъятие, медиана | цена в долях армии: p10 / медиана / p90 | разброс медиан по акторам |
|---|---|---|---|---|
| rome_375 | 96.2 % | 0.500 | 0.24 % / 0.96 % / 7.8 % | 0.23 % … 44.3 % = **190×** |
| constantinople_1430 | 84.2 % | 0.500 | 0.33 % / 3.2 % / 19.5 % | 0.29 % … 36.5 % = **124×** |
| milan_1477 | 98.1 % | 0.500 | 1.2 % / 5.1 % / **207 %** | 0.89 % … 208 % = **234×** |

`p90 = 207 %` в milan — не опечатка: изъятие превышает всю армию целиком, остаток
срезает кламп в ноль.

## 4. Структурный порог: кто не может держать армию вообще

С появлением источника армии (PR #62) равновесие считается в одну строку.
Восстановление добавляет `(C − m)·rate`, правило снимает `d`; равновесие —

```
(C − m)·rate = d   ⇒   m* = C − d/rate
```

При `d = 0.5` и `rate = 0.05` это `m* = C − 10`. Значит **любой актор с ёмкостью
ниже 10 не может держать армию ни при каких обстоятельствах**, а ёмкость
`C = 0.767·население^(2/3)` даёт границу по населению:

> **население < 47.1 ⇒ армия приколочена к нулю.**

Проверка предсказания против измерения — восемь акторов ниже границы:

| сценарий | актор | население (медиана) | ёмкость | армия (медиана) | тиков без армии |
|---|---|---|---|---|---|
| rome | saxons | 36.2 | 8.40 | 0.31 | 47.8 % |
| const | byzantium | 41.3 | 9.17 | 0.90 | 22.0 % |
| const | **wallachia** | 4.5 | 2.08 | **30.02** | **0.0 %** |
| milan | bologna | 42.4 | 9.33 | 0.00 | 56.2 % |
| milan | ferrara | 18.1 | 5.28 | 0.00 | 84.3 % |
| milan | mantua | 4.6 | 2.12 | 0.00 | 93.0 % |
| milan | siena | 32.2 | 7.76 | 0.00 | 61.9 % |
| milan | urbino | 15.5 | 4.76 | 0.00 | 81.5 % |

Предсказание сходится на семи из восьми, **и единственное исключение подтверждает
механизм**: `wallachia` — один из двух акторов, у которых правило не срабатывает ни
разу (§10 п. 3), и только он держит армию, вчетырнадцатеро превышающую его ёмкость.

Содержательная сторона: `urbino` без армии 81.5 % времени — это Урбино Федерико да
Монтефельтро, первого кондотьера эпохи. Модель армии это уже знала — в
`docs/investigation_military_source.md` §7 `urbino` назван среди акторов, которых
`k·pop^(2/3)` недооценивает. Абсолютное изъятие превращает недооценку в ноль.

## 5. Контрфакт «правило выключено» — верхняя граница влияния

| сценарий | гибели (актор-прогонов из 30 сидов) | актор-тиков без армии | армия, медиана | армия, p10 |
|---|---|---|---|---|
| rome, база | 153 | 3.7 % | 48.55 | 5.19 |
| rome, выкл | 120 | 0.0 % | 52.66 | 18.11 |
| const, база | 60 | 3.8 % | 19.33 | 2.40 |
| const, выкл | 60 | 0.6 % | 21.47 | 5.71 |
| milan, база | 60 | 29.3 % | 8.92 | 0.00 |
| milan, выкл | **30** | 1.4 % | 13.32 | 0.81 |

**Главный результат обхода: правило сегодня — единственный источник смертности
мелких акторов.** В milan оно даёт ровно половину всех гибелей: `siena` гибнет
30/30 в базе и 0/30 без правила. В rome оно стоит 33 гибели из 153 (`vandals`
19 → 6, `ostrogoths` 5 → 2, `alamanni` 23 → 18, `burgundians` 24 → 18).

Механизм прямой: `conquest_collapse` требует `military_size < 0.01` три тика
подряд. Ноль армии достижим **только** абсолютным изъятием — пропорциональное
затухает асимптотически и нуля не достигает.

**И ещё: полное снятие правила ломает ратифицированные ворота** — `rome scripted
influence` падает `3/3 → 2/3` (§7). Вариант «убрать» отклонён на измерении.

## 6. Кандидат: цена на запасе цели

Форма — зеркало `DeficitProportional`, которым задача 22 закрыла ту же болезнь на
`population`:

```
delta = − to · coefficient · (from − threshold) / threshold
```

При `threshold = 50` и `ep = 100` это `−to·coefficient`, то есть коэффициент
читается как **доля армии за тик при насыщенном давлении**. Равновесие становится
одинаковым для всех: `f = m*/C = 1/(1 + s/rate)`.

Свип (30 сидов × 300 тиков):

| вариант | rome гибели / без армии / армия med | const гибели / без армии / армия med | milan гибели / без армии / армия med |
|---|---|---|---|
| база (абсолютная) | 153 / 3.7 % / 48.55 | 60 / 3.8 % / 19.33 | 60 / 29.3 % / 8.92 |
| `s = 0.002` | 121 / 0.0 % / 49.62 | 60 / 0.6 % / 23.94 | 30 / 1.0 % / 13.06 |
| `s = 0.005` | 122 / 0.0 % / 48.74 | 59 / 0.6 % / 21.18 | 30 / 1.0 % / 12.47 |
| **`s = 0.01`** | **130 / 0.0 % / 47.72** | **60 / 0.6 % / 18.22** | **30 / 0.9 % / 11.72** |
| `s = 0.02` | 129 / 0.0 % / 42.20 | 60 / 0.6 % / 15.81 | 30 / 0.7 % / 10.74 |
| `s = 0.05` | 121 / 0.4 % / 28.61 | 60 / 0.6 % / 12.79 | 30 / 1.0 % / 8.08 |
| `s = 0.1` | 109 / 0.4 % / 18.31 | 60 / 0.6 % / 8.56 | 30 / 0.9 % / 5.44 |

**Смертность не восстанавливается ни при каком коэффициенте.** milan остаётся
30/60 на всём свипе; rome колеблется 109…130 против 153. Это не свойство выбора
`s`, а свойство формы: пропорциональное изъятие не даёт нуля.

Инверсия адресата видна поакторно (rome, `s = 0.01`): крупные начинают платить
больше (`eastern_jin` 0.500 → 1.890 за тик, `guptas` 0.500 → 1.626), мелкие —
почти ничего (`mantua` 0.500 → 0.005, milan).

## 7. Ратифицированные ворота

| вариант | const balanced ×5 | rome balanced ×3 | rome influence ×3 | rome wealth ×3 | milan aggressive |
|---|---|---|---|---|---|
| база | **5/5** (98/135/98/127/106) | 3/3 | 3/3 | 0/3 | без победы |
| правило выключено | 5/5 (95/99/113/106/83) | 3/3 | **2/3 ✗** | 0/3 | без победы |
| пропорциональная `s = 0.01` | 5/5 (90/94/117/176/98) | 3/3 | 3/3 | 0/3 | без победы |
| пропорциональная `s = 0.05` | 5/5 (136/225/262/212/161) | 3/3 | 3/3 | 0/3 | без победы |

При `s = 0.05` победа в constantinople сдвигается вдвое позже (османы дольше
держат армию, а условие победы — `ottomans.military_size < 40`). При `s = 0.01`
тики победы остаются в полосе базы.

## 8. Распределение, из которого выводится коэффициент

По правилу владельца (§10.5 п. 3 `investigation_military_source.md`) величина,
из которой выводится порог, публикуется **распределением**, а не средним.

`f = 1 − (изъятие/rate)/C` — доля ёмкости, на которой актор уравновешивается
сегодня:

| сценарий | акторов | q25 | **медиана** | q75 | мин | макс | с `f < 0` |
|---|---|---|---|---|---|---|---|
| rome_375 | 20 | 0.735 | **0.851** | 0.892 | −0.190 | 0.955 | 1 |
| constantinople_1430 | 9 | 0.268 | **0.567** | 0.665 | −0.091 | 0.948 | 1 |
| milan_1477 | 14 | −0.289 | **0.346** | 0.610 | −3.717 | 0.849 | 5 |

Сама эта таблица и есть дефект в числах: **у формы, которая должна была задавать
одно правило для всех, `f` разъезжается от 0.955 до −3.717**, и разъезжается
**по сценариям** — rome (крупные акторы) почти не задет, milan (мелкие) раздавлен.

Отсюда — коэффициент. `s = rate·(1/f − 1)`:

* сохранить медиану rome (`f = 0.851`) ⇒ `s = 0.0088`;
* сохранить медиану const (`f = 0.567`) ⇒ `s = 0.026`;
* сохранить медиану milan (`f = 0.346`) ⇒ `s = 0.094`.

Три разных числа — потому что «медиана сегодня» унаследовала перекос. **Брать их
нельзя: это значило бы вывести порог из искажения, которое правка устраняет.**
Годное утверждение одно и оно не про медиану, а про смысл: `s = 0.01` читается как
**«при насыщенном внешнем давлении держава теряет 1 % своей армии за полугодие»** и
даёт всем `f = 0.833`. Оно совпадает с сохранением медианы rome (0.0088 ≈ 0.01) —
но обосновано не совпадением, а тем, что это одно предложение о мире.

## 9. Развилка и рекомендация

**(A) Сменить форму на `ExcessProportional`, `s = 0.01`.**
Плюсы измерены: разброс цены 190/124/234× → 1× по построению; «армия ровно ноль»
3.7/3.8/29.3 % → 0.0/0.6/0.9 %; все ратифицированные ворота держатся; армия мира
почти не двигается в rome (48.55 → 47.72) и растёт в milan (8.92 → 11.72).
Цена названа и не заметается: **гибели rome 153 → 130, milan 60 → 30**
(`siena` перестаёт гибнуть совсем), `vandals` 19 → 9, `ostrogoths` 5 → 0.

**(B) Убрать правило.** Отклонён на измерении: ломает `rome influence` 3/3 → 2/3.

**(C) Отказ, форму не трогать.** Законный вариант: абсолютное изъятие — сегодня
**единственный** механизм проекта, который убирает державы, неспособные себя
защитить, и половина смертности milan держится на нём. Тогда §4 и §8 остаются
записанным свойством мира, а не дефектом.

**Рекомендация — (A).** Довод не в числах баланса, а в том, что́ правило говорит о
мире. Сегодня оно говорит: «внешнее давление отнимает у всех одинаковые 0.5 армии в
полугодие», и это утверждение неверно на собственных данных проекта — 0.5 для
Рима незаметно, а Урбино с населением 15 оно стирает начисто и держит стёртым.
Смертность, которая при этом теряется, **держалась на арифметической случайности**:
актор гибнет не потому, что его завоевали, а потому, что фиксированное число
оказалось больше его армии. Если мелкие державы должны гибнуть — это надо
сказать отдельным механизмом, а не получать побочным следствием шкалы.

**Развилка «смертность» отделена сознательно и не решается этой задачей.**

## 10. Найдено по дороге, не чинилось

1. **`budget_probe` хранит две рукописные копии `apply_dependency_rule`** (строки
   ~2353 и ~3688, плюс третья, частичная, ~1097). Найдены не чтением, а
   компилятором: добавление варианта перечисления сломало оба `match`. Кандидат в
   конвенционные гарды — по образцу гарда на типы `WorldState`.
2. **`economic_output_to_treasury`** — второй и последний член класса: абсолютная
   дельта до `−7.5` за тик на `treasury`, который не клампится ни снизу, ни сверху.
   Цена в долях запаса: медиана 2.8 % в rome, но p90 = **34 %**. Не измерялся.
3. **Два спавна constantinople правило не задевает ни разу:** `wallachia` и
   `poland_lithuania` — их `external_pressure` никогда не превышает 50, потому что
   спавн не задаёт этот ключ намеренно (задача 25). `wallachia` при этом держит
   армию 30.0 при ёмкости 2.08 — **армия, не связанная с населением вообще**.
4. `byzantium` (население 41.3, ёмкость 9.17) стоит **ниже структурного порога
   §4** — премисса сценария держится в том числе на нём.

## 11. Предложение критерия стадии 2 (двусторонний, объявляется до кода)

База — `main` на момент старта стадии 2, перемерить, **не наследовать числа из
этого документа** (мина задачи 30).

**Обязано выполниться:**
1. Разброс цены правила по акторам = 1.00× по построению во всех трёх сценариях.
2. Актор-тиков с `military_size < 0.01`: rome ≤ 0.5 %, const ≤ 1 %, milan ≤ 2 %.
3. Ни один актор с населением ниже 47 не имеет медианную армию 0.00.
4. Ратифицированные ворота: rome scripted balanced 3/3, influence 3/3, wealth 0/3;
   **constantinople scripted balanced 5/5** — при падении остановиться и доложить;
   milan aggressive без победы.
5. Тик победы constantinople в каждой из пяти партий — в полосе `база ± 60 %`.
6. Траектория армии протагониста каждого сценария наблюдается **отдельно**
   (мина PR #54: ратифицированные критерии к ней слепы).
7. `cargo test`, clippy `-D warnings`, детерминизм 8 процессов × 3 сценария.

**Обязано быть названо и принято владельцем как цена, а не скрыто:**
8. Гибели: ожидается rome ≈ 130 против 153, milan 30 против 60, const 60 без
   изменений. **Отклонение более чем на 15 % от ожидания — сигнал, что правка
   делает не то, что измерено здесь: остановиться и доложить, а не подкручивать `s`.**
9. Байт-в-байт недостижим и не требуется: правка меняет значения, значения меняют
   исходы боёв, исходы меняют число бросков. Это движковая механика (урок задачи 26).

**Чего делать нельзя:** подбирать `s` по пункту 8. Коэффициент выведен в §8 из
одного утверждения о мире; если он не проходит — падает утверждение, а не число.

## 13. Результат стадии 2: критерий сработал, правка НЕ влита

Правка выполнена ровно как согласовано и лежит на ветке
`investigate/pressure-military-form-stage2` (`de1c2ab`, **не мержить**): режим
`ExcessProportional` в движке, валидатор на положительный нормализатор, два
поведенческих теста, и одна строка в контенте каждого сценария —
`mode = "excess"` → `"excess_proportional"` при неизменном `coefficient = 0.01`.

### Сошлось восемь пунктов из девяти

| № | что | база | после | вердикт |
|---|---|---|---|---|
| 1 | разброс цены по акторам | 190× / 124× / 234× | **1.00× / 1.00× / 1.00×** | ✅ |
| 2 | актор-тиков с армией < 0.01 | 3.7 / 3.8 / 29.3 % | **0.0 / 0.6 / 0.9 %** | ✅ |
| 3 | акторов с медианной армией `0.00` | 5 | **0** | ✅ |
| 4 | ратифицированные ворота | 5/5, 3/3, 3/3, 0/3, без победы | **те же** | ✅ |
| 5 | тики победы constantinople | 98/135/98/127/106 | 90/94/117/176/98 | ✅ все в полосе |
| 6 | армия протагониста | 26.18 / 0.90 / 21.49 | 20.37 / 3.93 / 20.34 | ✅ (−22 %, предел −25 %) |
| 7 | `cargo test` | 88/88 | **87/88** | ❌ |
| 8 | clippy в форме CI | чисто | чисто | ✅ |
| 9 | гибели (цена, названа заранее) | 153 / 60 / 60 | **130 / 60 / 30** | ✅ совпало с ожиданием |

### Пункт 7: падает ратифицированный гард

`test_exhausted_actor_dies_to_an_armed_neighbour` — гард из PR #22/#23, который
утверждает: актор без армии, без легитимности, при насыщенном давлении и с
вооружённым соседом на границе обязан погибнуть.

Механизм установлен **трассировкой того же состояния под обеими формами**, а не
рассуждением:

| тик | старая форма: армия | новая форма: армия | ёмкость |
|---|---|---|---|
| 0 | 0.076 | 0.429 | 9.96 |
| 1 | **0.000** (счётчик 1) | 0.493 | 9.54 |
| 2 | **0.000** (счётчик 2) | 0.472 | 9.15 |
| 3 | **погиб** | 0.453 | 8.78 |
| 11 | — | 0.381 | 6.52 |

`conquest_collapse` требует `military_size < 0.01`. Пропорциональное изъятие берёт
1 % от того, что восстановление только что вернуло, — актор стоит на `0.36…0.49`
и в опасную зону не входит **никогда**.

### Сильнее, чем один тест: путь завоевания перестаёт существовать

Проверено выключением самого пути (порог 0, путь не может сработать):

| сценарий | сегодня: с путём → без пути | при форме (A): с путём → без пути |
|---|---|---|
| rome_375 | 153 → 141 (**путь даёт 12**) | 130 → 130 (**путь даёт 0**) |
| constantinople_1430 | 60 → 35 (**25**) | 60 → 60 (**0**) |
| milan_1477 | 60 → 30 (**30**) | 30 → 30 (**0**) |

**При форме (A) `conquest_collapse` не даёт ни одной гибели ни в одном сценарии.**
Оставшиеся 130/60/30 приходят из двух старых путей коллапса. Механизм, построенный
двумя PR, становится мёртвым кодом.

### Очевидная починка не работает, и это структурно

Напрашивается сделать и порог безразмерным: `military_size < доля · ёмкость`.
Измерено свипом доли (контрфакт `E13_CONQ_SHARE`, ветка измерения):

| доля | rome | constantinople | milan |
|---|---|---|---|
| 0.02 / 0.05 / 0.10 | 130 / 130 / 130 | 60 / 60 / 60 | 30 / 30 / 30 |
| 0.25 | 132 | 60 | 30 |
| 0.40 | 158 | 60 | 43 |
| 0.60 | **235** | 60 | **90** |
| *база `main`* | *153* | *60* | *60* |

Порог либо **ниже всех** (до 0.25 — путь остаётся мёртвым), либо **рядом со всеми
сразу** (0.40…0.60 — гибели взрываются). Промежутка нет, и это не вопрос точности
подбора: пропорциональная форма ставит **всех** акторов на одну и ту же долю
ёмкости `f = 0.833`, поэтому любой порог относительно ёмкости пересекает либо
пустое множество, либо почти всё население мира разом. **Та самая однородность,
которая чинит перекос цены, лишает предикат смертности различающей способности.**

### Вывод стадии 2

**В этом движке смертность мелких держав и есть шкальная асимметрия.** Это одна
вещь, а не две: актор гибнет ровно потому, что фиксированное число оказалось
больше его армии. Починить перекос и сохранить смертность **одной правкой одного
правила невозможно** — это тот же класс сцепления, что PR #22/#23 (починка
фантомного боя молча убивала всю смертность), только обнаруженный с другой стороны.

Стадия 1 назвала цену числами (гибели 153 → 130, 60 → 30) и на этом основании
рекомендовала (A). Стадия 2 показала, что за этими числами стоит **исчезновение
механизма**, а не его ослабление. Это новый факт, и он меняет вес развилки.

### Развилка, решение за владельцем

1. **(C) — отказ.** Форму не трогать. §4 (порог населения 47) и §8 (разброс `f` от
   0.955 до −3.717) остаются записанным свойством мира. Правка на ветке остаётся
   как готовое свидетельство.
2. **(A) + признать путь завоевания мёртвым.** Влить форму, удалить
   `conquest_collapse` и его гард как более не работающие. Это отменяет
   содержательную часть PR #22/#23; объём заметно больше согласованного.
3. **(A) + новый носитель смертности** — отдельная задача с собственной стадией 1.
   Видимые отсюда кандидаты: смертность по накопленным боевым потерям, а не по
   мгновенному размеру армии; либо по населению, которое в этом движке уже имеет и
   источник, и сток.

**Рекомендация: (1) сейчас, (3) отдельной задачей.** Влить (A) без носителя
смертности — значит обменять измеримый перекос на потерю механизма, а этот обмен
стадией 1 не оценивался и владельцем не согласовывался.

### Ветки

* `investigate/pressure-military-form` (`4d213dd`) — оснастка стадии 1, NOT FOR MERGE.
* `investigate/pressure-military-form-measure` (`8ce4fd4`) — та же оснастка поверх
  `main` плюс контрфакт `E13_CONQ_SHARE`, NOT FOR MERGE.
* `investigate/pressure-military-form-stage2` (`de1c2ab`) — **готовая правка (A)**,
  87/88 тестов, не мержить без решения по развилке.

## 14. Решение владельца и поправки к §13

**Решение (2026-09-13): вариант (1) — отказ.** Форма не меняется. Правка остаётся
на ветке `investigate/pressure-military-form-stage2` (`de1c2ab`) как готовое
свидетельство и не вливается. Вариант (3) — новый носитель смертности — заводится
отдельной задачей со своей стадией 1. Вариант (2) отклонён: вливать форму, зная,
что `conquest_collapse` становится мёртвым кодом, значит сознательно завести в
движок мёртвую подсистему ради обмена, который стадия 1 не оценивала.

Владелец воспроизвёл падение независимо: на `de1c2ab` — 87 passed, 1 failed, падает
именно `test_exhausted_actor_dies_to_an_armed_neighbour`, и в диффе к `main` этого
теста нет. Равновесие пересчитано по формуле в движке: `f = 1/(1 + 0.01/0.05) = 0.833`.

### 14.1 Критерий поймал не тем местом, которым гордился

Пункт 9 — «гибели, отклонение больше 15 % означает стоп» — **не сработал бы**:
гибели вышли `130 / 60 / 30`, ровно как предсказала стадия 1, с нулевым отклонением.
Задачу остановил **ратифицированный гард из чужого PR** — тест, утверждающий про
мир («истощённый актор с вооружённым соседом обязан погибнуть»), а не про величину.

**Число сошлось, смысл разошёлся.** Это правило метода, а не случай этой задачи:
критерий, целиком составленный из величин, не ловит исчезновение механизма, потому
что исчезновение проявляется как *ожидаемое* число. Отсюда два следствия для всех
будущих критериев:

* **вклад подсистемы меряется её выключением, а не разницей итогов.** Именно
  выключение пути (`130/60/30` и с ним, и без него) превратило «гибели упали на
  15 %» в «путь не срабатывает ни разу»;
* в критерий, меняющий механику, обязан входить хотя бы один пункт, **утверждающий
  про мир**, а не про число. Здесь эту роль сыграл чужой тест — случайно.

### 14.2 «Все на одной доле» — равновесие, а не распределение

Формулировка §13 неточна и спотыкала бы следующего читателя. При точном тождестве
`m/C = 0.833` порог `0.60·ёмкость` не поймал бы **никого**, и rome остался бы на
`130` — а наблюдается `235`. Значит разброс есть, и он измерен (Часть 5 пробника,
30 сидов × 300 тиков, форма (A)):

| сценарий | p10 | медиана | p90 | поакторные медианы |
|---|---|---|---|---|
| rome_375 | 0.429 | **0.819** | 1.024 | 0.238 (`rome`) … 0.987 (`huns`) |
| constantinople_1430 | 0.460 | **0.834** | 1.433 | 0.457 (`venice`) … 33.3 (`mamluks`) |
| milan_1477 | 0.283 | **0.538** | 0.921 | 0.278 (`mantua`) … 0.833 (`france`) |

Доля актор-тиков ниже порога, заданного долей собственной ёмкости:

| порог | rome | constantinople | milan |
|---|---|---|---|
| 0.10 | 0.03 % | 1.59 % | 0.04 % |
| 0.25 | 4.31 % | 2.93 % | 5.69 % |
| 0.40 | 8.55 % | 5.50 % | 23.51 % |
| 0.60 | **27.73 %** | **24.71 %** | **59.84 %** |

**Верная формулировка: пропорциональная форма даёт не тождество, а сгущение вокруг
общего равновесия — и вывод от этого крепче, а не слабее.** Порог по доле ёмкости
оказывается либо ниже всего сгущения (до 0.25 — путь мёртв, гибели неподвижны), либо
режет по самому сгущению (0.60 — четверть и более всех актор-тиков сразу, гибели
235 и 90). Разделяющей полосы нет не потому, что все равны, а потому, что все
собраны в одном месте шкалы, по которой предлагается их различать.

### 14.3 Предупреждение к стадии 1 задачи (3)

Население как носитель смертности выглядит удобнее боевых потерь, но
`economic_output_to_population` и `low_economic_output_to_population_decay` **уже
переведены в `deficit_proportional` задачей 22**. Население уже живёт в
пропорциональном режиме, поэтому предикат по доле населения рискует упереться в ту
же однородность уровнем выше — и §14.2 придётся переписывать про другую метрику.

**Накопленные боевые потери этим свойством не обладают: это величина с историей, а
не мгновенная доля запаса.** Начинать стадию 1 задачи (3) следует с них.

### 14.4 Найдено при этом замере, не чинилось

Все **три** спавна constantinople стоят вне канала давления (их `external_pressure`
не превышает 50 никогда, ключ не задан намеренно — задача 25), а их население при
этом падает до нуля обычным стоком:

| актор | авторское население | медианное население | ёмкость | армия |
|---|---|---|---|---|
| `mamluks` | 800 | **2.0** | 1.22 | 65.08 |
| `wallachia` | 250 | **2.8** | 1.52 | 30.02 |
| `poland_lithuania` | 700 | 536.4 | 50.64 | 58.77 |

`mamluks` держит армию **53×** своей мобилизационной ёмкости, `wallachia` — **20×**,
и обе неподвижны до конца партии. Расширяет §10 п. 3: это не единичный случай, а
свойство всех спавнов сценария.

## 15. Поправка к §14.4 и правило про квантор

Владелец нашёл в §14.4 ошибку. Она принята и исправлена ниже; при перепроверке
выяснилось, что вторая половина его замечания объясняется не тем, что он
предположил, — это тоже записано, потому что неверное объяснение переживает
верную поправку.

### 15.1 Квантор был на шаг шире измерения

§14.4 писал: «Все **три** спавна constantinople стоят вне канала давления, **а их
население при этом падает до нуля** обычным стоком». Первая половина верна. Вторая
противоречит таблице в том же абзаце: `poland_lithuania` держит `536.4` из
авторских `700`.

Измерено (30 сидов × 300 тиков, форма (A) — тот же прогон, что в §14.2):

| актор | авторское население | медианное | ёмкость | армия | `median(m/C)` |
|---|---|---|---|---|---|
| `mamluks` | 800 | **2.0** | 1.22 | 65.08 | **33.3** |
| `wallachia` | 250 | **2.8** | 1.52 | 30.02 | **8.76** |
| `poland_lithuania` | 700 | 536.4 | 50.64 | 58.77 | **1.00** |

**Верно так: вне канала давления стоят все три спавна, но аномалия «армия, не
связанная с населением» — у двух из трёх.** `poland_lithuania` держит ровно свою
ёмкость и ничем не выделяется.

### 15.2 Третий случай одной и той же ошибки

Это не описка, а повторяющаяся форма. Три случая подряд в одном документе:

| место | сказано | измерено |
|---|---|---|
| §10 п. 3 | «два спавна не задевает правило» | их **три** |
| §13 | «все акторы на одной доле `0.833`» | **сгущение**, p10 `0.283…0.460` |
| §14.4 | «все три, и население падает до нуля» | до нуля падает **у двух** |

Каждый раз измерение точное, а фраза захватывает на шаг шире измеренного — в обе
стороны: дважды обобщение оказалось у́же истины, один раз шире.

> **Правило: квантор в тексте не шире и не уже, чем в таблице под ним.** Слова
> «все», «каждый», «ни один», «единственный» проверяются пересчётом строк таблицы,
> на которую они опираются, а не памятью о том, что показывал замер.

Записано рядом с правилом §14.1 про выключение подсистемы: оба про то, что текст
расходится с измерением не в числах, а в связках между ними.

### 15.3 Два числа про `mamluks` — не режимы, а две статистики

§14.2 даёт `mamluks` `33.3`, §14.4 — `53×`. Предположение, что таблицы взяты из
разных режимов (форма (A) и база), **проверено и не подтвердилось**: оба числа из
одного и того же прогона формы (A). Расходятся они потому, что это разные величины:

* `median(m / C)` — медиана потиковой доли: **33.3**;
* `median(m) / median(C)` — частное двух медиан из Части 3: **53.5**.

Они совпадают, пока обе величины стоят на месте, и расходятся вдвое, когда ёмкость
едет, а армия нет: население `mamluks` падает `800 → 2.0`, армия стоит на `65`.
У `wallachia` та же пара даёт `8.76` против `19.7`, у обычных акторов — `0.457`
против `0.436` и `0.862` против `0.862`.

Пробник (ветка измерения, `da3859c`) теперь печатает **обе** статистики рядом,
поакторно, чтобы соседние таблицы нельзя было прочитать как разные режимы.

### 15.4 Найдено при перепроверке: правка (A) включала спавн, который сегодня не срабатывает

`mamluks` **не появляется ни разу** в базовых прогонах (0 из 30 сидов × 300 тиков,
оба независимых базовых замера) и появляется при форме (A). Его веха ждёт
`ottomans.cohesion < 40` — порог, недостижимый сегодня и достижимый при
пропорциональном изъятии.

То есть отклонённая правка не только убивала путь `conquest_collapse`, но и
**оживляла авторский спавн, который в текущем мире мёртв**. На решение это не
влияет — оно уже принято, — но при возврате к теме это ещё одна величина, которую
придётся назвать заранее, а не обнаружить после.

## 16. Вторая поправка: утверждение про мир обязано называть мир

Владелец нашёл, что §15.4 обнуляет вывод §15.1, а правило §15.2 этого не ловит.
Принято целиком; ниже исправления и правило-спутник.

### 16.1 Что было неверно

Жирный вывод §15.1 — «аномалия у **двух из трёх**» — истинен **в мире формы (A)**.
Таблица над ним помечена режимом, само утверждение — нет. В мире `main`, который
отгружается, спавнов существует **два**, а аномальный — **один**.

Той же породы последняя строка §14.4, которую первая поправка не тронула:
«Расширяет §10 п. 3: это не единичный случай, а свойство **всех спавнов**
сценария». **На `main` это ровно единичный случай — `wallachia`**, как стадия 1 и
написала.

Сведение по мирам:

| | объявлено в контенте | существует на `main` | существует при форме (A) |
|---|---|---|---|
| спавнов constantinople | 3 | **2** | 3 |
| из них с армией, не связанной с населением | — | **1** (`wallachia`, `median(m/C) = 7.34`) | 2 (`wallachia` 8.76, `mamluks` 33.3) |

`poland_lithuania` на `main` держит `median(m/C) = 1.47`, при форме (A) — `1.00`;
аномальным его не считаем ни там, ни там.

### 16.2 §15.2 разобрал один из трёх случаев неверно

Строка «§10 п. 3 сказал *два спавна*, измерено *три*» записана как ошибка счёта.
**Это не ошибка.** Стадия 1 мерила базу, где `mamluks` не существует, и «два» для
базы **верно**. §14.4 мерил форму (A), где спавнов три, и «три» там тоже **верно**.
Два истинных утверждения про разные миры, неотличимые, пока мир не назван.

Исправленная сводка случаев:

| место | сказано | измерено | природа |
|---|---|---|---|
| §13 | «все акторы на одной доле `0.833`» | сгущение, p10 `0.283…0.460` | квантор шире замера |
| §14.4 | «все три, и население падает до нуля» | до нуля падает у двух | квантор шире замера |
| §10 п. 3 ↔ §14.4 | «два спавна» ↔ «три спавна» | оба верны | **мир не назван** |
| §15.1 | «аномалия у двух из трёх» | верно для (A), неверно для `main` | **мир не назван** |

Два разных дефекта, а не три случая одного.

### 16.3 Правило-спутник

> **Утверждение про мир несёт имя мира — «в базе» или имя контрфакта — в самом
> предложении, а не только в шапке таблицы над ним.**

Проверка такая же механическая, как у правила §15.2: в предложении со словами
«все», «ни один», «единственный», «впервые» должно стоять либо «в базе», либо имя
режима. Оба правила про одно: ошибка сидит не в числе, а в связке между числом и
тем, о чём оно. **Квантор — связка «сколько», режим — связка «где».**

### 16.4 Самостоятельная находка про `main`: недостижимая веха с оплаченным контентом

Проверено переписью **всех** вех со `spawn_actor` в трёх сценариях (Часть 6
пробника, `db84dd5`; 30 сидов × 300 тиков, контент `main`) — чтобы не повторить ту
же ошибку квантора на одном примере:

| сценарий | спавн | вошёл в мир | дожил до конца | ворота | наблюдалось |
|---|---|---|---|---|---|
| constantinople | `wallachia` | 30/30 | **0/30** | `ottomans.military_size > 70` | 95.2 … 310.5 |
| constantinople | `poland_lithuania` | 30/30 | 13/30 | `tick == 5` | — |
| constantinople | **`mamluks`** | **0/30** | 0/30 | `ottomans.cohesion < 40` | **47.97 … 100.00** |
| milan | `france` | 30/30 | 30/30 | `savoy.external_pressure > 55` | 46.1 … 100.0 |
| rome | — | — | — | сценарий не спавнит никого | — |

**`mamluks` — авторский контент, который в отгружаемой игре мёртв.** Держава с
населением 800, армией 65 и собственной вехой не появляется ни разу за 30 партий,
потому что когезия османов не опускается ниже `47.97` при пороге `40`. Разрыв
восемь пунктов — промах калибровки, а не заведомо невозможное условие.

Это дефект `main`, **существующий независимо** от отклонённой правки и от всей этой
задачи. В §15.4 он записан как свойство правки («оживляла спавн»); правильная
формулировка обратная и адресована владельцу: **в игре сегодня простаивает
оплаченный контент.** Не в объёме задачи, не чинится, заведено «за скобками» —
но как находка про `main`, иначе при следующем заходе его найдут снова и снова как
побочное следствие.

Попутно перепись показала второе: **`wallachia` входит в мир во всех 30 партиях и
не доживает до конца ни в одной.** Это другое состояние, чем у `mamluks`, и первая
версия счётчика их спутала бы — она мерила присутствие в конце прогона и сказала бы
про `wallachia` «не входит ни разу». **Мина метода: «сколько партий актор
существует» и «в скольких он жив в конце» — разные величины; на спавнах они
расходятся полностью.**
