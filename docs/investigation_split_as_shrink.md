# `(A₃)` «раскол как усадка»: ворота не пройдены — посылка ложна на `main`

**Статус:** стадия 1 выполнена, **ворота не пройдены**, правок нет. Причина — не в конструкции `(A₃)`, а в том, что на сегодняшнем `main` у Рима к тику вехи нет армии, которую можно делить. Найден и измерен дефект, который это вызвал; он же — источник трёх прежних выводов, которые здесь исправляются (§5).
**Эталон:** `main` = `a409fa7`. Пробник `split_probe` (варианты `shrink_formula` / `shrink_keep`, строки `COH`/`WEST`), 30 сидов × 300 тиков; сравнение «до/после PR #54» — бинарники `sim` с `c0d7a9e` (до) и `e5a1f8e` (после).
**Постановка:** `investigation_triggers_collapse.md` §5–6/§8: актор условия `rome_splits` остаётся Западом (× 0.45 по формуле архитектуры), рождается только `rome_east` (× 0.55 из метрик *живого* родителя), 82 сайта контента и лимес остаются валидными.

## 1. Что измерялось

`split_probe` на тике срабатывания `rome_splits` эмулирует усадку между тиками (движок не тронут): Рим переименовывается по шаблону `rome_west`, метрики режутся формулой раскола с долей 0.45 (`shrink_formula` — с «травмой раскола»: `cohesion 20`, `legitimacy 30`, `ep × 1.3`; `shrink_keep` — Запад сохраняет свои `cohesion`/`legitimacy`/`ep`, режутся только доли); `rome_east` рождается по формуле с долей 0.55 из метрик родителя, с авторскими рёбрами и обратной записью соседям (правило PR #52).

## 2. Результат усадки — инертен

| | `none` (эталон) | `shrink_formula` | `shrink_keep` |
|---|---|---|---|
| тик усадки | — | медиана 20 (веха 6…30) | та же |
| **Запад сразу после усадки** (среднее, 30 партий) | — | `mil` **10.3**, `pop` 3278, `treasury` 5342, `coh` 20, `leg` 30, `ep` 99 | `mil` **10.3**, `coh` 3.6, `leg` 42, `ep` 97.5 |
| Запад на тике 300 | — | жив 30/30; `mil` 5.3, `leg` 18.8, `ep` 100 | жив 30/30; `mil` 3.8 |
| `rome_east` на тике 300 | — | жив 30/30; `mil` 3.2, `leg` **0.0**, `ep` 100, живых соседей d1 **0** | то же |
| гибели варваров лимеса (visigoths/vandals/franks/alamanni/burgundians) | 15/16/16/17/17 | 17/16/13/16/14 | 18/15/14/16/14 |

Формула делит **армию в 10 человек**: в момент вехи Рим уже разоружён. Обе половины рождаются беззащитными бессмертными, мир не меняется. `(A₃)` не опровергнута — она **не проверяема**, пока посылка «у Рима есть что делить» ложна.

## 3. Почему у Рима нет армии: PR #54 разоружил его за 15 лет

| | до #54 (`c0d7a9e`) | после #54 (`main`) |
|---|---|---|
| `rome.military_size` на тике 10 / 30 / 100 (сид 0) | 347 / 342 / **314** | **97 / 3.6 / 0.0** |
| тик, на котором `military_size < 1` | не наступает | **30/30 партий, медиана 31** (23…71) |
| боёв за партию (сиды 0…4) | 17 / 23 / 24 / 22 / 18 | **64 / 57 / 44 / 68 / 73** |
| `rome.cohesion < 30` в первые 30 тиков (сетка 5 тиков, 10 сидов) | **2/10** | **10/10** (тик 5…25) |

**Механизм — прочитан по коду.** `calculate_military_conflict` (`interactions.rs:452–466`): потери — **фиксированные доли собственной армии**, атакующий 5…15 %, защитник 15…30 %, независимо от соотношения сил; защитник ещё теряет `cohesion` 10…20 и получает `ep` +15…25 за бой. Пять рёбер лимеса дают Риму пять фронтов и ~60 боёв за партию: как атакующий (армия 350 против 18…48) он платит 17…52 человека за бой, варвар — 3…14; после ~30 боёв Рим пуст, становится защитником, и каждый бой снимает ему 10…20 сплочённости — отсюда ранняя веха. Легитимность Рим не теряет ниже **19.5**: ранг S держит пол `20.0` (`rank_bonuses.toml`), и ни один путь гибели (`leg < 10`) не открывается — вот всё «бессмертие Рима».

Это класс задачи 5/17 (`investigation_combat_asymmetry.md`: «атакующий платит 5…15 % за бой»), впервые проявившийся на протагонисте: потеря пропорциональна **своей** армии, а не силе противника, и крупная держава проигрывает войну на истощение пяти племенам суммарной численностью 150.

## 4. Перерасчёт порога вехи (по траектории `cohesion` на `main`)

| порог / длительность | срабатывает | медианный тик |
|---|---|---|
| `< 30` / 5 (сейчас) | 30/30 | 20 |
| `< 30` / 10 · 20 | 29 · 23 /30 | 25 · 35 |
| `< 25` / 5 · 10 · 20 | 29 · 29 · 18 | 20 · 25 · 35 |
| `< 20` / 5 · 10 · 20 | 29 · 29 · 14 | 21 · 28 · 124 |

Порог ничего не калибрует: сплочённость рушится боями с лимесом на 10…25-м тике при любом разумном пороге. Часы вехи — производная от §3, не самостоятельный параметр.

## 5. Исправления к прежним выводам (объект — мои собственные документы)

1. **`investigation_d1_graph.md` §5, §9.2 «Рим по-прежнему не гибнет».** Верно, но скрывает главное: после набора лимеса Рим **разоружён к тику 31 в 30/30 партий**, а не гибнет только из-за пола легитимности ранга S. Критерий стадии 2 там (§7) не включал траекторию армии протагониста — ратифицированный критерий rome (победа семьи на тике 31…103) к ней слеп по построению. Добавлено примечанием в тот документ.
2. **`investigation_consequence_context.md` §2.1** объяснял раннее переключение rome (тик 7…28) «скачками сплочённости от случайных событий». До #54 сплочённость уходила под 30 в первые 30 тиков в 2 партиях из 10, после — в 10/10: раннее переключение и 94 % промптов `Consequences` — **следствие #54**, а не свойство сценария. Правка (B) `(PR #56)` от этого не зависит (премиса нужна в любом режиме), но база 2814 — число мира после #54.
3. **`investigation_triggers_collapse.md` §2 и §5 п.1** — «сценарный период 4…15 лет» — то же: мир после #54. До #54 веха срабатывала редко и поздно (сид 42: тик 262).

## 6. Вердикт и что дальше

**`(A₃)` — отложена, не отклонена.** Постановка остаётся; её ворота — мир, где у Рима есть армия к моменту вехи.

**Предпосылка — отдельная задача: модель потерь в бою.** Потеря доли своей армии независимо от противника (`0.05…0.15` / `0.15…0.30`) — дефект формы, не калибровки: он делает крупную державу самой уязвимой к множеству слабых соседей. Кандидаты для её стадии 1 (формулируются, не выбираются): (i) потери пропорциональны армии **противника** (обмен), (ii) потери зависят от соотношения сил, (iii) потолок потерь за бой в абсолютных единицах. Развилка мерится тем же `siege_probe`/`split_probe` + `sim` scripted (ратифицированные победы). **До неё**: ни `(A₃)`, ни калибровка `rome_splits`, ни новые рёбра d1 — все три упираются в §3.

**PR #54 остаётся**: рёбра лимеса верны исторически, а разоружение Рима — свойство модели потерь, которое они лишь обнажили; откат вернул бы бессмертных варваров и мёртвые шаблоны наследников, не починив модель.

## 7. За скобками
- Пол легитимности ранга S (`20.0`) как единственный источник бессмертия Рима — после починки потерь пересмотреть вместе с предикатами гибели протагониста.
- `rome.legitimacy` держится на 19.5, а не 20.0 — пол применяется до какого-то вычитания в 0.5; не разбиралось.

---

## 11. Перемер после источника армии (2026-09-12): **ворота `(A₃)` пройдены**

**Эталон:** `main` = `a038529` (после PR #62, источник армии в движке). Свидетельство — ветка `investigate/shrink-remeasure` (`515fe9a`, **NOT FOR MERGE**): переключатель `E13_SHRINK=1` реализует усадку прямо в движке, поэтому меряются и безыгроковый мир, и `sim`. При пустом окружении выход побайтово равен `main`.

Стадия 1 отказала по одной причине: к тику вехи Риму было нечего делить. Эта посылка проверена заново.

### 11.1 Ворота: теперь есть что делить

| в момент срабатывания `rome_splits` | до источника (§2) | после источника |
|---|---|---|
| армия Рима | **10.3** | **91.2** |
| население | 3278 | 7235 |
| сплочённость / легитимность / давление | — | 11.6 / 30.9 / 96.8 |

**Веха сдвинулась сама, без калибровки порога:** срабатывает в **27 партиях из 30** (было 30/30), медианный тик **82** вместо **20**, разброс 6…168. Год 416 против 385; исторический раздел — 395, тик 40. Порог `cohesion < 30` при длительности 5 не трогался: сплочённость роняет оборона, а Рим с восстановленной армией реже оказывается защитником.

### 11.2 Что даёт усадка (30 сидов × 300 тиков, движковый переключатель)

| | без усадки | усадка |
|---|---|---|
| `rome_east` рождается | — | **27/30**, медианный тик 82 |
| `rome` (Запад) гибнет | 0/30 | **0/30** — остаётся в мире, все 82 сайта контента продолжают адресовать живого актора |
| Запад на тике 290 | армия 30.4, сплочённость 7.4, легитимность 17.6 | армия **24.2**, сплочённость 7.3, легитимность 17.8 |
| Восток на тике 300 | — | жив **27/27**, армия **183.6**, население 3778 — на своей ёмкости |
| доля тиков, где Рим беззащитен (`mil < 0.01 ∧ leg < 10`) | 0.0 % | 0.0 % |
| гибели | `alamanni` 24, `armenia` 23, `burgundians` 25, `franks` 21, `huns` 30, `late_sassanids` 7, `ostrogoths` 5, `sassanids` 7, `vandals` 18 | `alamanni` 20, `armenia` 30, `burgundians` 23, `franks` 11, `huns` 30, `ostrogoths` 7, `vandals` 15 |

Переворота нет: сдвиги умеренные, ни один актор не уходит в `0/30` или `30/30` из середины, кроме `armenia` (23 → 30) и исчезновения редких гибелей `sassanids`/`late_sassanids`.

**Историческое чтение — в пользу `(A₃)`.** Запад остаётся слабым обломком (армия 24), Восток становится настоящей державой (армия 184, на ёмкости своего населения). Это и есть 476 против 1453, и получается само, без единого подогнанного числа: доли `0.45 / 0.55` авторские, формула раскола архитектурная, ёмкость выведена задачей об источнике.

### 11.3 Находка метода: ратифицированные ворота rome **слепы к `(A₃)`**

`sim scripted` для rome завершается досрочно по победе на тиках **31…82**, а веха срабатывает на медиане **82**. Поэтому все девять ратифицированных прогонов rome под усадкой **побайтово идентичны** прогонам без неё — гейт не видит правку вовсе. Это тот же класс, что мина PR #54: критерий, который не может отличить правку от её отсутствия, не является её проверкой.

Для стадии 2 это означает: рома-ворота остаются в критерии **как требование побайтовой идентичности** (они обязаны не измениться, потому что заканчиваются раньше), но **проверкой правки служить не могут**; проверять придётся безыгроковый мир и отдельный прогон scripted без досрочного выхода.

### 11.4 Развилка стадии 2 (сформулирована, не выбрана)

1. **Кто остаётся в мире.** В измерении остался первый объявленный наследник (`rome_west`, доля 0.45), потому что там живёт семья игрока (Медиолан). Правилу нужна опора: порядок объявления — хрупкая; кандидаты — явное поле в контенте либо «остаётся тот, чей шаблон несёт столицу игрока».
2. **Травма раскола для остающегося.** Мерилось с травмой на обе половины (`cohesion 20`, `legitimacy 30`, `ep × 1.3`); Запад после неё падает до сплочённости 0.1 за один тик. Альтернатива — травма только новорождённому, остающийся сохраняет свои `cohesion`/`legitimacy`.
3. **Имя и идентичность.** Остающийся переименовывается по шаблону (`Западная Римская империя`), сохраняя `id = rome`: это то, что удерживает 82 сайта контента и полигон карты. Нужно решить, верно ли это как конструкция, а не только как удобство.
4. **Текст вехи.** PR #58 сделал `llm_context_shift` честным («сплочённость рухнула, держава цела»). С `(A₃)` держава действительно раскалывается, и текст возвращается к расколу — но уже правдиво.
5. **Порог `rome_splits`.** Медиана 82 против исторических 40. Калибровать или принять как есть — отдельное решение; свип стадии 1 (§4) измерялся на мире без источника и **устарел**.

### 11.5 Критерий стадии 2 (объявлен до кода; эталон `a038529`)

**Обязано измениться:** `rome_east` рождается в ≥ 20 партиях из 30; `rome` не гибнет ни в одной; армия Востока на тике 300 выше армии Запада не менее чем в 3 раза в ≥ 20 партиях (измерено 183.6 против 24.2).
**Обязано остаться:** rome scripted balanced / influence / wealth × сиды 42/1/7 — **побайтово** (обоснование §11.3: заканчиваются до вехи); constantinople и milan — побайтово (их вехи `triggers_collapse` наследников не имеют); ни один актор не уходит в `0/30` или `30/30` оттуда, где был в середине, **кроме перечисленных с объяснением** (формулировка §10.4); `cargo test`, clippy `-D warnings`; детерминизм 8 процессов × 3 сценария.
**Наблюдать отдельной строкой:** долю промптов в режиме последствий (§11.6 — она уже изменилась и изменится снова).

### 11.6 Перемер чисел, которые устарели от источника армии

| число | где опубликовано | было | стало на `a038529` |
|---|---|---|---|
| армия Рима: тик обнуления | `investigation_d1_graph.md` §9.2, `investigation_split_as_shrink.md` §3 | `0` к тику 31 в 30/30 | **не обнуляется ни разу**; медиана на тике 290 — 14.2 |
| `rome_splits`: срабатывания и тик | `investigation_triggers_collapse.md` §2 | 30/30, медиана 20 | **27/30, медиана 82** |
| доля промптов в режиме последствий, rome | `investigation_consequence_context.md` §2.1, `investigation_triggers_collapse.md` §2 | **94 %** (2814/3000), переключение на тиках 7…28 | **59.3 %** (1780/3000), переключение 7…126, **в 3 партиях из 10 веха не срабатывает вовсе** |
| «беззащитные бессмертные» | `investigation_besieged_reach.md` §2, `investigation_d1_graph.md` §9.2 | 32.1 / 30.3 / 11.1 %, затем 26.8 / 26.1 / 10.8 % | **3.7 / 3.0 / 0.0 %** |
| свип порога `rome_splits` | §4 этого документа | таблица на мире без источника | **устарела целиком**, перемерять вместе с решением по §11.4 п.5 |

Вывод §2–§6 этого документа («ворота не пройдены, предпосылка — модель потерь») остаётся верным **для своего мира** и заменён этим разделом: предпосылка выполнена, ворота пройдены.
