# Cargo configuration for ENGINE13 (Tauri crate)
#
# Здесь НЕ должно быть абсолютных путей: до 2026-09-21 файл закреплял
# rustc = "/home/deck/.cargo/bin/rustc", из-за чего крейт не собирался
# нигде, кроме одной машины, и CI не мог его проверить.

[build]
incremental = true
